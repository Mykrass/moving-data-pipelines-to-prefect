import setuptools

setuptools.setup(
    name='monstermash', # Repalace with your own username
    version='0.0.1',
    author='Mykras',
    author_email='mykras@me.com',
    description='My custom ML monsters in a package',
    packages=setuptools.find_packages(),
    python_requires='>=3.8',
)